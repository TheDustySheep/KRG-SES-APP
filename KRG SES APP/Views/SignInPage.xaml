<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="KRG_SES_APP.Views.SignInPage"
             NavigationPage.HasNavigationBar="False"
             Style="{StaticResource standardContentPage}">

    <ContentView
        Style="{StaticResource standardContentView}">
        <StackLayout>
            <Label Text="Sign In System" Style="{StaticResource pageHeaderLabel}"/>
            <StackLayout>
                <Label x:Name="LastSignIn" Text="Refresh For Last Update"/>
                <StackLayout x:Name="LoggedOutStack" Orientation="Horizontal" VerticalOptions="CenterAndExpand" HorizontalOptions="CenterAndExpand">
                    <Button Text="Sign In" Style="{StaticResource buttonImportant}" Clicked="OnSignIn" HorizontalOptions="Center"/>
                    <Button Text="Refresh" Style="{StaticResource buttonImportant}" Clicked="OnRefresh" HorizontalOptions="Center"/>
                </StackLayout>
            </StackLayout>
        </StackLayout>
    </ContentView>
    
</ContentPage>